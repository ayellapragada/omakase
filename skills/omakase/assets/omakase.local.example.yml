# Personal project preferences and verified machine-local knowledge. Never commit this file.
version: 1

# Optional purpose-keyed tool routes; see project guidance for examples.
tools: {}

# Explicit standing workflow preferences for this project only.
preferences: []

bootstrap:
  # Ordered, idempotent steps required in every fresh worktree.
  steps:
    - runtime:
      run:
      env: {}
      needs: []

validation:
  quick:
    - runtime:
      run:
      env: {}
      needs: []
  full: []

worktree:
  # Record paths only. Never put secret values in this file.
  required_local_files: []
  notes: []

ci:
  provider:
  required_checks: auto

publication:
  base_branch:
  merge: manual
