interface:
  display_name: "Omakase"
  short_description: "Deliver project changes with lean, risk-based review"
  default_prompt: "Take this project change from intake through isolated implementation, validation, pull request, CI, and review follow-up. Use a lean review cycle by default, escalating review intensity only when risk justifies the additional model use. Ask only when a material decision or new authority is required."
